import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.amuchat.app',
  appName: 'AMU-Chatbot',
  webDir: 'dist/amu-chatbot'
};

export default config;
